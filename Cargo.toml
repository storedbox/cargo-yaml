# Auto-generated from "Cargo.yaml"

[dependencies]
docopt = "*"
rustc-serialize = "*"
toml = "*"
yaml-rust = "*"

[package]
authors = ["David Huffman <storedbox@outlook.com>"]
description = "Drop-in Cargo subcommand to generate a Cargo.toml manifest from an YAML template."
homepage = "https://gitlab.com/storedbox/cargo-yaml/"
keywords = ["cargo", "file", "generate", "generator", "manifest", "subcommand", "toml", "yaml"]
license = "MIT/Apache-2.0"
name = "cargo-yaml"
publish = false
readme = "README.md"
repository = "https://gitlab.com/storedbox/cargo-yaml.git"
version = "3.0.0-devel"

[profile]

[profile.bench]
lto = true

[profile.release]
lto = true

[profile.test]
lto = true
