# Auto-generated from `Cargo.yaml`

[dependencies]
toml = "0.1.30"
yaml-rust = "0.3.3"

[package]
authors = ["David Huffman <storedbox@outlook.com>"]
description = "A drop-in Cargo subcommand which generates a Cargo.toml manifest from an YAML template."
keywords = ["cargo", "file", "generate", "generator", "manifest", "subcommand", "toml", "yaml"]
license = "MIT/Apache-2.0"
name = "cargo-yaml"
publish = false
version = "2.1.0-prealpha"

[profile]

[profile.bench]
lto = true

[profile.release]
lto = true

[profile.test]
lto = true
